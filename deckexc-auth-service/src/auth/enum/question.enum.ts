/**
 * Enumeración que representa las posibles preguntas de seguridad que un usuario 
 * puede seleccionar durante el proceso de registro.
 * Cada valor corresponde a una pregunta que el usuario puede usar para recuperar su cuenta.
*/

export enum Question {
  COMIDA = 'comida',
  CANTANTE = 'cantante',
  PAIS = 'pais',
}
